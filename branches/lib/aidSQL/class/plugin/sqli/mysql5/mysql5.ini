ending-payloads		=	"ORDER BY 1"
comment-payloads		=	"/*_--_#"
field-payloads			=	"_'_%'_')_%')"		;use double quotes!
injection-attempts	=	40
;strings-only						;Only grab numeric fields (option needs no value)
;numeric-only						;Only grab numeric fields (option needs no value)
;start-offset		=	0
;var-count			=	0			;Max variables to test per link
